# Example env for local server run
# Option A: provide minimal FIREBASE_* vars (recommended for Vercel-like env)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# Replace actual newlines with \n when pasting into Vercel or PowerShell env var
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"

# Option B: alternatively paste the full JSON into this var
# GOOGLE_APPLICATION_CREDENTIALS_JSON={...}

# Other useful envs
BASE_API_URL=https://turvia.com.br
MERCADO_PAGO_ACCESS_TOKEN=
